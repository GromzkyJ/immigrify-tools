name: Deploy to Production

on:
  push:
    branches:
      - test
      - main

jobs:
  deploy:
    runs-on: ubuntu-latest
    
    steps:
    - name: Checkout code
      uses: actions/checkout@v3
    
    - name: Setup Node.js
      uses: actions/setup-node@v3
      with:
        node-version: '18'
    
    - name: Setup PHP and Composer
      uses: shivammathur/setup-php@v2
      with:
        php-version: '8.1'
        extensions: mbstring, xml, zip, gd, curl, mysqli, pdo_mysql
        tools: composer
    
    - name: Install Composer Dependencies for SuiteCRM
      run: |
        if [ -f "crm/composer.json" ]; then
          echo "Installing Composer dependencies for SuiteCRM..."
          cd crm
          composer install --no-dev --optimize-autoloader --no-interaction --prefer-dist
          cd ..
        fi
    
    - name: Build React Apps
      run: |
        # Find all directories with package.json (React apps)
        for dir in */; do
          if [ -f "${dir}package.json" ]; then
            dirname=$(basename "$dir")
            echo "Building React app: ${dirname}"
            cd "${dir}"
            
            echo "  Installing dependencies..."
            if ! npm install; then
              echo "  ✗ ERROR: npm install failed for ${dirname}"
              exit 1
            fi
            
            echo "  Building app..."
            if ! npm run build; then
              echo "  ✗ ERROR: npm run build failed for ${dirname}"
              exit 1
            fi
            
            # Verify build directory was created
            if [ ! -d "build" ]; then
              echo "  ✗ ERROR: build directory not created for ${dirname}"
              exit 1
            fi
            
            # Verify build has files
            if [ -z "$(ls -A build 2>/dev/null)" ]; then
              echo "  ✗ ERROR: build directory is empty for ${dirname}"
              exit 1
            fi
            
            echo "  ✓ Successfully built ${dirname}"
            echo "  Build contains $(find build -type f | wc -l) files"
            cd ..
          fi
        done
    
    - name: Prepare build directory
      run: |
        mkdir -p build
        
        echo "Available directories:"
        ls -la
        
        # Copy simple HTML apps (folders without package.json)
        echo "=== Starting to copy directories ==="
        for dir in */; do
          dirname=$(basename "$dir")
          echo "Checking directory: ${dirname}"
          
          # Skip if it's a React app (has package.json) or special folders
          if [ -f "${dir}package.json" ]; then
            echo "  → Skipping ${dirname} (has package.json - React app)"
            continue
          fi
          
          if [ "$dirname" == "node_modules" ] || [ "$dirname" == ".git" ] || [ "$dirname" == "build" ] || [ "$dirname" == ".github" ] || [ "$dirname" == "docs" ] || [ "$dirname" == "examples" ]; then
            echo "  → Skipping ${dirname} (special folder)"
            continue
          fi
          
          echo "  → Copying ${dirname} to build/"
          if cp -r "${dir}" build/; then
            echo "  ✓ Successfully copied ${dirname}"
            echo "  Verifying copy:"
            if [ -d "build/${dirname}" ]; then
              echo "    ✓ build/${dirname} exists"
              file_count=$(find "build/${dirname}" -type f 2>/dev/null | wc -l)
              echo "    ✓ Contains ${file_count} files"
            else
              echo "    ✗ ERROR: build/${dirname} does not exist after copy!"
            fi
          else
            echo "  ✗ ERROR: Failed to copy ${dirname}"
          fi
        done
        
        echo "Build directory contents after copying:"
        ls -la build/
        
        # Configure SuiteCRM .htaccess for subdirectory deployment
        if [ -d "build/crm" ] && [ -f "crm/.htaccess" ]; then
          echo "Configuring .htaccess for SuiteCRM subdirectory"
          # Update RewriteBase - will be set correctly in deployment step
          # Default to test, production step will override if needed
          cp crm/.htaccess build/crm/.htaccess
        fi
        
        # Copy React app build outputs
        for dir in */; do
          dirname=$(basename "$dir")
          if [ -f "${dir}package.json" ]; then
            echo "Processing React app: ${dirname}"
            if [ -d "${dir}build" ]; then
              echo "  Found build directory"
              # Copy the build folder contents to the app folder in build/
              mkdir -p "build/${dirname}"
              
              # Check if build directory has content
              if [ "$(ls -A "${dir}build" 2>/dev/null)" ]; then
                echo "  Copying build contents..."
                # Use rsync for atomic, reliable copying - handles hidden files automatically
                # --no-links prevents symlink issues that cause FTP errors
                if command -v rsync >/dev/null 2>&1; then
                  rsync -av --no-links "${dir}build/" "build/${dirname}/"
                  echo "  ✓ Copy completed (using rsync)"
                else
                  # Fallback: use tar which preserves everything and handles hidden files
                  echo "  rsync not available, using tar fallback..."
                  (cd "${dir}build" && tar -cf - .) | (cd "build/${dirname}/" && tar -xf -)
                  echo "  ✓ Copy completed (using tar fallback)"
                fi
              else
                echo "  ✗ ERROR: ${dirname}/build directory is empty - build may have failed"
                exit 1
              fi
              
              # Verify copy was successful AND check for broken references
              if [ -d "build/${dirname}" ] && [ "$(ls -A "build/${dirname}" 2>/dev/null)" ]; then
                file_count=$(find "build/${dirname}" -type f 2>/dev/null | wc -l)
                echo "  ✓ Successfully copied ${dirname} build (${file_count} files)"
                
                # Check for and remove any symlinks (FTP doesn't handle them well)
                symlink_count=$(find "build/${dirname}" -type l 2>/dev/null | wc -l)
                if [ "$symlink_count" -gt 0 ]; then
                  echo "  ⚠ Found $symlink_count symlinks, removing them to prevent FTP issues..."
                  find "build/${dirname}" -type l -delete
                  echo "  ✓ Symlinks removed"
                fi
                
                # Check for asset-manifest.json and verify static folder exists
                if [ -f "build/${dirname}/asset-manifest.json" ]; then
                  if [ ! -d "build/${dirname}/static" ]; then
                    echo "  ⚠ Warning: asset-manifest.json exists but static folder is missing"
                  else
                    static_files=$(find "build/${dirname}/static" -type f 2>/dev/null | wc -l)
                    echo "  ✓ Static folder verified ($static_files files)"
                  fi
                fi
                
                # Verify critical files were copied (especially index.html for React apps)
                if [ ! -f "build/${dirname}/index.html" ]; then
                  echo "  ✗ ERROR: index.html not found in build/${dirname}/"
                  echo "  Listing contents:"
                  ls -la "build/${dirname}/" | head -20 || true
                  exit 1
                else
                  echo "  ✓ Verified: index.html exists"
                fi
              else
                echo "  ✗ ERROR: Failed to copy ${dirname} build - destination is empty"
                exit 1
              fi
            else
              echo "  ✗ ERROR: ${dirname}/build directory does not exist - build may have failed"
              exit 1
            fi
          fi
        done
        
        # Copy root level HTML/CSS/JS files
        cp -r *.html *.css *.js build/ 2>/dev/null || true
        cp -r public build/ 2>/dev/null || true
    
    - name: Configure .htaccess for Test Environment
      if: github.ref == 'refs/heads/test'
      run: |
        if [ -f "build/crm/.htaccess" ]; then
          echo "Setting RewriteBase for test environment"
          sed -i 's|RewriteBase /tools-test/crm/|RewriteBase /tools-test/crm/|g' build/crm/.htaccess
          sed -i 's|RewriteBase /tools/crm/|RewriteBase /tools-test/crm/|g' build/crm/.htaccess
        fi
    
    - name: Verify build directory
      if: github.ref == 'refs/heads/test'
      run: |
        echo "=== Build Directory Verification ==="
        echo "Checking if build directory exists..."
        if [ ! -d "build" ]; then
          echo "ERROR: Build directory does not exist!"
          exit 1
        fi
        
        echo "Build directory contents:"
        ls -la build/ || echo "Cannot list build directory"
        
        echo "All folders in build/:"
        find build/ -maxdepth 1 -type d | sort
        
        echo "Checking for React apps..."
        for dir in build/*/; do
          if [ -d "$dir" ]; then
            dirname=$(basename "$dir")
            echo "Checking ${dirname}..."
            if [ -f "build/${dirname}/index.html" ] || [ -f "build/${dirname}/static" ]; then
              echo "  ✓ ${dirname} appears to be a valid app (has index.html or static folder)"
              file_count=$(find "build/${dirname}" -type f 2>/dev/null | wc -l)
              echo "  ✓ Contains ${file_count} files"
            else
              echo "  ⚠ ${dirname} might be incomplete (no index.html or static folder)"
            fi
          fi
        done
        
        echo "Checking for CRM folder (if present)..."
        if [ -d "build/crm" ]; then
          echo "✓ CRM folder found in build/"
          echo "CRM folder contents (first 20 items):"
          ls -la build/crm/ | head -20
          echo "Total files in crm: $(find build/crm -type f 2>/dev/null | wc -l)"
        else
          echo "ℹ CRM folder not found (this is OK if not deploying CRM)"
        fi
        
        echo "=== Verification Complete ==="
    
    - name: Pre-FTP Verification
      if: github.ref == 'refs/heads/test'
      run: |
        echo "=== Pre-FTP Verification ==="
        echo "Checking build directory structure..."
        echo "Total items in build/:"
        find build/ -maxdepth 1 -type f -o -type d | wc -l
        
        echo "Listing all top-level items in build/:"
        ls -la build/
        
        echo "Checking for React apps in build/:"
        for app_dir in build/*/; do
          if [ -d "$app_dir" ]; then
            app_name=$(basename "$app_dir")
            echo "  App: ${app_name}"
            if [ -f "${app_dir}index.html" ]; then
              echo "    ✓ Has index.html"
            fi
            if [ -d "${app_dir}static" ]; then
              echo "    ✓ Has static folder"
            fi
            file_count=$(find "$app_dir" -type f 2>/dev/null | wc -l)
            echo "    Total files: ${file_count}"
          fi
        done
        
        echo "=== Pre-FTP Verification Complete ==="
        
        # Ensure build directory is not empty
        if [ -z "$(ls -A build/ 2>/dev/null)" ]; then
          echo "ERROR: Build directory is empty!"
          exit 1
        fi
        
        # List all files that will be uploaded (for debugging)
        echo "=== Files to be uploaded ==="
        find build/ -type f | head -50
        echo "Total files: $(find build/ -type f | wc -l)"
        
        # Check for and remove symlinks (FTP doesn't handle them well)
        echo "Checking for symlinks..."
        symlinks=$(find build/ -type l 2>/dev/null | wc -l)
        if [ "$symlinks" -gt 0 ]; then
          echo "  ⚠ Found $symlinks symlinks, removing them to prevent FTP errors..."
          find build/ -type l -delete
          echo "  ✓ Symlinks removed"
        else
          echo "  ✓ No symlinks found"
        fi
        
        # Final verification: ensure no broken file references
        echo "Performing final file integrity check..."
        for app_dir in build/*/; do
          if [ -d "$app_dir" ] && [ -f "${app_dir}index.html" ]; then
            app_name=$(basename "$app_dir")
            # Check if static folder exists if asset-manifest.json exists
            if [ -f "${app_dir}asset-manifest.json" ] && [ ! -d "${app_dir}static" ]; then
              echo "  ⚠ Warning: ${app_name} has asset-manifest.json but no static folder"
            fi
          fi
        done
        
        echo "Build directory is ready for deployment"
        
        # Final check: ensure contact-form has index.html
        if [ -d "build/contact-form" ]; then
          if [ -f "build/contact-form/index.html" ]; then
            echo "✓ contact-form/index.html exists"
          else
            echo "✗ ERROR: contact-form/index.html is missing!"
            echo "Files in contact-form:"
            ls -la build/contact-form/ || true
            exit 1
          fi
        fi
    
    - name: Deploy to Test Environment
      if: github.ref == 'refs/heads/test'
      uses: SamKirkland/FTP-Deploy-Action@4.3.0
      with:
        server: ${{ secrets.FTP_SERVER }}
        username: ${{ secrets.FTP_USERNAME }}
        password: ${{ secrets.FTP_PASSWORD }}
        local-dir: ./build/
        server-dir: ./tools-test/
        protocol: ftp
        port: 21
        dangerous-clean-slate: false
        log-level: verbose
        exclude: |
          **/.git*
          **/.git*/**
          **/node_modules/**
          **/tests/**
          **/.github/**
          **/.travis.yml
          **/.codecov.yml
          **/codacy.yml
          **/codeception.dist.yml
          **/phpcs.xml
          **/.php_cs.dist
          **/.php_cs.cache
          **/RoboFile.php
          **/crm/build/**
          **/vendor/build/**
          **/travis.php.ini
          **/README.md
          **/LICENSE.txt
          **/.env.dist
          **/.gitattributes
          **/composer.lock
          **/bower_components/**
          **/.sass-cache/**
          **/tests/_output/**
          **/tests/_envs/**
          **/codeception.yml
          **/BrowserStackLocal
          **/browserstack.err
          **/fakeImapResource
          **/include/Imap/ImapTestSettings.txt
          **/themes/SuiteP/css/*.map
          **/themes/SuiteP/css/*/*.map
          **/themes/SuiteP/css/*/color-palette.css
          **/themes/SuiteP/css/*/variables.css
    
    - name: Configure .htaccess for Production Environment
      if: github.ref == 'refs/heads/main'
      run: |
        if [ -f "build/crm/.htaccess" ]; then
          echo "Setting RewriteBase for production environment"
          sed -i 's|RewriteBase /tools-test/crm/|RewriteBase /tools/crm/|g' build/crm/.htaccess
          sed -i 's|RewriteBase /tools/crm/|RewriteBase /tools/crm/|g' build/crm/.htaccess
        fi
    
    - name: Verify build directory
      if: github.ref == 'refs/heads/main'
      run: |
        echo "Checking build directory contents..."
        ls -la build/ || echo "Build directory does not exist"
        if [ -d "build/crm" ]; then
          echo "CRM folder found in build/"
          ls -la build/crm/ | head -20
          echo "Total files in crm: $(find build/crm -type f | wc -l)"
        else
          echo "ERROR: CRM folder not found in build/"
          exit 1
        fi
    
    - name: Deploy to Production Environment
      if: github.ref == 'refs/heads/main'
      uses: SamKirkland/FTP-Deploy-Action@4.3.0
      with:
        server: ${{ secrets.FTP_SERVER }}
        username: ${{ secrets.FTP_USERNAME }}
        password: ${{ secrets.FTP_PASSWORD }}
        local-dir: ./build/
        server-dir: ./tools/
        protocol: ftp
        port: 21
        dangerous-clean-slate: false
        exclude: |
          **/.git*
          **/.git*/**
          **/node_modules/**
          **/tests/**
          **/.github/**
          **/.travis.yml
          **/.codecov.yml
          **/codacy.yml
          **/codeception.dist.yml
          **/phpcs.xml
          **/.php_cs.dist
          **/.php_cs.cache
          **/RoboFile.php
          **/crm/build/**
          **/vendor/build/**
          **/travis.php.ini
          **/README.md
          **/LICENSE.txt
          **/.env.dist
          **/.gitattributes
          **/composer.lock
          **/bower_components/**
          **/.sass-cache/**
          **/tests/_output/**
          **/tests/_envs/**
          **/codeception.yml
          **/BrowserStackLocal
          **/browserstack.err
          **/fakeImapResource
          **/include/Imap/ImapTestSettings.txt
          **/themes/SuiteP/css/*.map
          **/themes/SuiteP/css/*/*.map
          **/themes/SuiteP/css/*/color-palette.css
          **/themes/SuiteP/css/*/variables.css

